name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Build single-file YAMLs
        run: python build.py

      - name: Verify build is idempotent
        run: |
          # dist/ is git-ignored, so `git diff` cannot see it. Snapshot
          # the first build, rerun, and diff the two directories. Any
          # drift (content, added, or removed files) fails the check.
          cp -r dist dist-first
          python build.py
          diff -ruN dist-first dist || {
            echo 'build.py is not idempotent - dist/ differs between runs';
            exit 1;
          }
          rm -rf dist-first

      - name: Sanity-check dist/
        run: |
          # Expected count is data-driven from the DEVICES list in
          # build.py, so adding a variant there is the only edit
          # required to keep this check honest.
          expected=$(grep -c '\.yaml\.src",' build.py)
          count=$(ls dist/*.yaml | wc -l)
          if [ "$count" -ne "$expected" ]; then
            echo "expected $expected YAML files in dist/ (per build.py DEVICES), found $count"
            ls -l dist/
            exit 1
          fi
          for f in dist/*.yaml; do
            size=$(stat -c%s "$f")
            if [ "$size" -lt 50000 ]; then
              echo "$f is suspiciously small: $size bytes"
              exit 1
            fi
          done

      - name: Upload build artefacts
        uses: actions/upload-artifact@v4
        with:
          name: ghmonitorgizmo-firmware
          path: dist/*.yaml
          if-no-files-found: error
