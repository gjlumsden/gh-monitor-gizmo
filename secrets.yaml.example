# Wi-Fi (shared across all ESPHome devices)
wifi_ssid: "your-ssid"
wifi_password: "your-wifi-password"

# GitHub API
# Personal access token used only to raise the rate limit on the
# public /users/{user}/events endpoint.
#   - Fine-grained PAT: "Metadata: Read-only" is sufficient (and is
#     granted by default). Add repo access if you want private-repo
#     events to appear.
#   - Classic PAT: no scopes needed for public events; add `repo` for
#     private repo activity.
ghgizmo_github_user: "your-github-username"
ghgizmo_github_token: "github_pat_xxx_or_ghp_xxx"

# ESPHome device secrets
# Generate a new API key: https://esphome.io/components/api.html
ghgizmo_api_encryption_key: "base64-32-byte-key"
# Any password you like; used for OTA updates
ghgizmo_ota_password: "some-long-random-string"
# Fallback AP password shown if Wi-Fi can't connect
ghgizmo_ap_password: "some-other-random-string"
